import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MyColors {
  static const Color primaryColor = Color.fromRGBO(15, 20, 60, 0.96);
  static const Color textColor = Colors.white;
  static const Color secColor = Color.fromRGBO(244, 67, 54, 1);
}